---
type Props = {
	title: string;
	description: string;
	tags?: string[];
	href?: string;
};

const { title, description, tags, href } = Astro.props;
---

<a
	href={href}
	class='w-full not-prose border rounded-md group blend bg-background/80 hover:bg-foreground/10 space-y-2 flex p-4'
>
	<div class='flex w-full justify-between'>
		<div class='flex space-y-1 flex-col items-start'>
			<h2 class='font-bold text-xl'>{title}</h2>
			<p class='text-foreground/80 text-md'>{description}</p>
		</div>
		<svg class='size-5 text-foreground/50 group-hover:text-muted-foreground'>
			<use href='/ui.svg#open'></use>
		</svg>
	</div>
</a>
